import React from "react"

export default () => (
  <div style={{ color: `tomato`, width: `100%` }}>
    <h1 style={{ fontSize: `48px`, textAlign: `center` }}>Hello Walter!</h1>
    <p style={{ fontSize: `24px`, textAlign: `center` }}>What a world!</p>
  </div>
)
